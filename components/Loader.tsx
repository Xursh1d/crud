import { Loader as TableLoader } from "@gravity-ui/uikit"

export default function Loader() {
    return (
        <div className="w-full flex justify-center py-10">
            <TableLoader size="m" />
        </div>)
}
